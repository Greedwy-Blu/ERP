import { View } from 'react-native';
import FuncionarioCreateScreen from '@/screens/gestor/FuncionarioCreateScreen'; // Ajuste o caminho

export default function funcionarioCreateScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#f8fafc' }}>
      <FuncionarioCreateScreen />
    </View>
  );
}
