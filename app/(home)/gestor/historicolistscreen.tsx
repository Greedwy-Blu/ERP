import { View } from 'react-native';
import HistoricoListScreen from '@/screens/gestor/HistoricoListScreen'; // Ajuste o caminho

export default function historicolistScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#f8fafc' }}>
      <HistoricoListScreen />
    </View>
  );
}
