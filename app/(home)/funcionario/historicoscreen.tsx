
import { View } from 'react-native';
import HistoricoScreen from '@/screens/funcionario/HistoricoScreen'; // Ajuste o caminho

export default function funcionarioghistoricoScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#f8fafc' }}>
      <HistoricoScreen />
    </View>
  );
}
