import { View } from 'react-native';
import PerfilScreen from '@/screens/funcionario/PerfilScreen'; // Ajuste o caminho

export default function funcionarioperfilScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#f8fafc' }}>
      <PerfilScreen />
    </View>
  );
}
