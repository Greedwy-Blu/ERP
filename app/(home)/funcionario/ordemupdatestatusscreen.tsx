import { View } from 'react-native';
import OrdemUpdateStatusScreen from '@/screens/funcionario/OrdemUpdateStatusScreen'; // Ajuste o caminho

export default function funcionarioordemupdateScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#f8fafc' }}>
      <OrdemUpdateStatusScreen />
    </View>
  );
}
